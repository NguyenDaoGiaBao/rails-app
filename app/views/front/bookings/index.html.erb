<div class="row justify-content-center">
  <div class="col-md-8">
    <h2 class="text-center mb-4">🎬 Đặt Vé Xem Phim</h2>

    <% if flash[:notice] %>
      <div class="alert alert-success">
        <%= flash[:notice] %>
      </div>
    <% end %>

    <%= form_with url: front_booking_path, method: :post, local: true, data: { turbo: false } do |f| %>
      <div class="mb-3">
        <%= label_tag :movie, "Chọn phim:", class: "form-label" %>
        <%= select_tag :movie, options_for_select(@movies), prompt: "Chọn phim", class: "form-select", required: true %>
      </div>

      <div class="mb-3">
        <%= label_tag :time, "Chọn giờ chiếu:", class: "form-label" %>
        <%= select_tag :time, options_for_select(@timeslots), prompt: "Chọn giờ", class: "form-select", required: true %>
      </div>

      <div class="text-center">
        <%= submit_tag "🎟️ Đặt vé ngay", class: "btn btn-primary px-4 py-2" %>
      </div>
    <% end %>
  </div>
</div>
